
# 1. `SimpleAttention` 내 Q, K, V 개별 선언 방식과 역할 분담

- 기초 실습에서 학습한 Q,K,V 생성 과정과 통합 코드의 구현 방식을 비교하여 분석

- Query, Key, Value의 생성부터 Attention Score 계산, Softmax, Value 결합까지의 전체 흐름을 코드와 연결하여 분석

- 기초 실습의 Self-Attention이 `SimpleAttention`에서 어떤 형태로 확장되었는지 자신의 관점에서 설명

# 2. 베셀 보정 없는 층 정규화(LayerNorm)와 학습 안정성

- 통합 코드에서 직접 구현한 `LayerNorm`의 계산 과정과 주요 구성요소를 분석

- `mean`, `var`, `eps`, `scale`, `shift`가 각각 어떤 역할을 하는지 코드와 연결하여 설명

- 기초적인 정규화 개념이 Transformer 구조 안에서 어떤 의미를 갖는지 자신의 관점에서 분석

# 3. 활성화 함수와 피드포워드 신경망 (GELU 및 FeedForward)

- `FeedForward`의 구조와 차원 변화 과정을 코드와 연결하여 분석

- `GELU`가 전체 구조에서 어떤 역할을 하는지 설명

- Attention과 FeedForward가 각각 어떤 역할을 수행하는지 비교하여 분석

# 4.  숏컷 연결(Residual Connection)과 `TransformerBlock`의 결합

- `TransformerBlock`을 구성하는 주요 요소와 이들의 연결 관계를 분석

- Attention, LayerNorm, FeedForward, Residual Connection이 어떤 순서와 방식으로 결합되어 있는지 코드와 연결하여 설명

- 기초 실습의 단일 Attention이 하나의 TransformerBlock으로 확장되면서 어떤 구조적 변화가 발생했는지 분석

# 5. 전체 LLM 아키텍처 파이프라인의 완성 (`MiniGPT`와 `CONFIG`)

- 입력 문장이 Token ID로 변환된 이후 모델을 통과하는 전체 데이터 흐름을 분석

- Token Embedding, Position Embedding, TransformerBlock, Final LayerNorm, Output Head의 연결 관계를 코드와 함께 분석

- 현재 설정값(`vocab_size`, `context_length`, `emb_dim` 등)이 모델의 입력과 출력 구조에 어떤 영향을 미치는지 분석

- 최종 Logit이 생성되고 다음 단어 후보를 선택하는 과정까지 포함하여 전체 MiniGPT Pipeline의 동작을 자신의 관점에서 설명